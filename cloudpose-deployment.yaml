apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudpose-deployment
spec:
  # replicas: 1 # 作业要求一开始先使用1个pod，后续再扩展
  # replicas: 2 # 现在是第二轮压测，先扩展到2个pod
  replicas: 8 # 现在是第三轮压测，直接扩展到8个pod
  selector:
    matchLabels:
      app: cloudpose
  template:
    metadata:
      labels:
        app: cloudpose
    spec:
      containers:
        - name: cloudpose
          image: coolguyinurarea/cloudpose:v2
          ports:
          - containerPort: 60000
          resources:
            requests:
              cpu: "0.5"
              memory: "1024Mi"
            limits:
              cpu: "0.5"
              memory: "1024Mi"
          